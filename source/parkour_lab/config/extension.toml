[package]

# Semantic Versioning is used: https://semver.org/
version = "0.1.0"

# Description
category = "isaaclab"
readme = "docs/README.md"

title = "Parkour Lab"
author = "Leon Yi Bai"
maintainer = "Leon Yi Bai"
description = "Adaptive parkour curriculum and reproducible evaluation for a Unitree A1 in Isaac Lab"
repository = "https://github.com/HalloLeon/parkour-lab.git"
keywords = ["isaaclab", "reinforcement-learning", "parkour", "unitree-a1"]

[dependencies]
"isaaclab" = {}
"isaaclab_assets" = {}
"isaaclab_rl" = {}
"isaaclab_tasks" = {}

[[python.module]]
name = "parkour_lab"

[isaaclab_settings]
# Optional system dependencies can be declared here when needed.
# apt_deps = ["example_package"]
# ros_ws can point to a ROS workspace with rosdep-managed dependencies.
# ros_ws = "path/from/extension_root/to/ros_ws"
